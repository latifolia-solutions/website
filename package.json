{
  "name": "latifolia-solutions-site",
  "version": "1.0.0",
  "private": true,
  "description": "Latifolia Solutions – Cybersecurity and AI Security product development consulting website (static export for GitHub Pages)",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "rm -rf docs && mkdir -p docs && cp -R out/* docs/ && touch docs/.nojekyll && sh -c 'test -f CNAME && cp -f CNAME docs/CNAME || true' 2>/dev/null || true && mkdir -p docs/assets && cp -R public/assets/* docs/assets/ 2>/dev/null || true",
    "lint": "next lint"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "next": "^15.4.7",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/react": "^18.2.79",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.7",
    "typescript": "^5.5.3"
  }
}
